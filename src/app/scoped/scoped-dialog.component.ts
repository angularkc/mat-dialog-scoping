import {Component, OnInit} from '@angular/core';

@Component({
  selector: 'sst-scoped-dialog',
  template: `<p>scoped-dialog works!</p>`
})
export class ScopedDialogComponent implements OnInit {

  constructor() {
  }

  ngOnInit() {
  }

}
